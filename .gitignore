venv/
staticfiles/
db.sqlite3
__pycache__/
*.py[cod]
.env

# Build Android
mobile/android/build/
mobile/android/debug.keystore
mobile/android/*.apk
mobile/android/*.idsig

# Cle privee de signature des licences (NE JAMAIS COMMITTER)
mobile/license-private-key.pem

# Codes de licence emis (a ne pas publier)
mobile/codes-*.txt

# Commerce Control : isole sur la branche "commerce", exclu du depot public.
# (conserve sur le disque — dont la cle privee — mais jamais pousse depuis main)
/commerce/
